model WaitlistEntry {
  id        String   @id @default(cuid())
  email     String   @unique
  name      String
  cellphone String
  domain    String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([domain])
}
